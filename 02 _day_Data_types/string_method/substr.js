// it is similar to slice()
// The difference is that the second parameter specifies the length of the extracted part.

// substr(start, length)

let str = "i am learning JavaScript";
console.log(str.substr(5, 5));    // it return part of string from starting index to the given length
// learn

console.log(str.substr(-10));   //return the 10 character from the last of the string