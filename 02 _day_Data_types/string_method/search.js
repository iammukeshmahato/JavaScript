// search() method is used to searches a string for a specific value
// returns the index of matched character

// string.search("value")

let string = "hello world hello";
console.log(string.search("hello"));    // 0    -> returns the index of first match

// as indexOf() & lastIndexOf(), search doesn't takes second parameter