// indexOf method returns the index of the character
// returns -1 if match not found

// string.indexOf("char")

let string = "this is me mukesh mahato";
console.log(string.indexOf("m"));   // 8
console.log(string.indexOf("mukesh"));   // 11
console.log(string.indexOf("mahato"));   // 18

// only returns the first occurance or match

// methods accept a second parameter as the starting position for the search:
